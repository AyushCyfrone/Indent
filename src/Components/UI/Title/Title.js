import React from "react";

function Title({ Title }) {
  return (
    <div class="flex">
      <div className="text-xl font-semibold mb-[5px]">{Title}</div>
    </div>
  );
}

export default Title;
