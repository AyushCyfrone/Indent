// import React from "react";
// import { useAuth } from "../AuthContext"; 


// import { existingdraft, rawIngredients, inventoryItems, image, uom, parlevel } from "../AuthContext"; 


//     function getImageSrcById(id) {
//         const { image } = useAuth(); // Destructure image from useAuth
//         const itemsrc = image.find(item => item.id === id);
//         return itemsrc ? itemsrc : null;
//     }
    
//     function getUOMById(id) {
//         const { uom } = useAuth(); // Destructure uom from useAuth
//         const itemsrc = uom.find(item => item.id === id);
//         return itemsrc ? itemsrc : null;
//     }
    
//     function getRawIngredientById(id) {
//         const { rawIngredients } = useAuth(); // Destructure rawIngredients from useAuth
//         const itemsrc = rawIngredients.find(item => item.id === id);
//         return itemsrc ? itemsrc : null;
//     }
    
//     function getInventoryItemById(id) {
//         const { inventoryItems } = useAuth(); // Destructure inventoryItems from useAuth
//         const itemsrc = inventoryItems.find(item => item.id === id);
//         return itemsrc ? itemsrc : null;
//     }
    
//     function getParlevleById(id) {
//         const { parlevel } = useAuth(); // Destructure parlevel from useAuth
//         const itemsrc = parlevel.find(item => item.id === id);
//         return itemsrc ? itemsrc : null;
//     }


// export default {getImageSrcById, getUOMById, getRawIngredientById, getInventoryItemById, getParlevleById}; 